export const HREF = window.location.protocol + '//' + window.location.hostname + (window.location.port.length === 0 ? '' : (':' + window.location.port)) + window.location.pathname;

// export const HOST = window.location.protocol + '//' + window.location.hostname + (window.location.port.length === 0 ? '' : (':' + window.location.port));

// export const HOST = 'http://192.168.199.112';

export const PORT = '8080';

export const ERR_OK = 0;
